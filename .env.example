# ここに書いたキーは、デプロイ時にランダムな16進48文字で .env に補完される。
# したがって「値を選べないキー = 秘密情報」だけを置くこと。
# DB_HOST / DB_NAME / DB_USER のような固定値は docker-compose.yml の environment に持つ。

# MySQL
DB_PASS=
DB_ROOT_PASS=

# 管理画面のログインパスワード。ログインIDは admin 固定。
# 生成された値はサーバー上の .env から読み出して利用者に共有する。
ADMIN_PASSWORD=
